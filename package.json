{
  "name": "test-grant1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ test-grant1",
    "create-local": "graph create --node http://localhost:8020/ test-grant1",
    "remove-local": "graph remove --node http://localhost:8020/ test-grant1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 test-grant1",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.48.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
